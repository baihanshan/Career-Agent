OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
BGE_MODEL_NAME=BAAI/bge-large-zh-v1.5
# 可选：自定义 BGE 模型缓存目录。保持注释则使用项目内默认路径 .local/bge-models
# （首次运行会自动下载 BGE 模型到此目录）
# BGE_MODEL_CACHE_DIR=
# 可选：自定义 Chroma 持久化目录。保持注释则使用项目内默认路径 .local/chroma
# CHROMA_PATH=
APP_ENV=development
